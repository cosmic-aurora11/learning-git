---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const recaps = await getCollection('recaps');
const sortedRecaps = recaps.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
const tags = Array.from(new Set(sortedRecaps.flatMap((recap) => recap.data.tags))).sort();
const timelineRecaps = sortedRecaps.filter((recap) => [5, 4].includes(recap.data.sessionNumber));
const useTimelineLayout = true;
---

<BaseLayout title="Recaps | The Arcane Atlas">
  <section class="section page-hero">
    <div class="container">
      <h1>Session Recaps</h1>
      <p class="muted">Relive the story so far.</p>
      {useTimelineLayout ? (
        <div class="timeline" style="margin-top:2rem;">
          {timelineRecaps.map((recap) => (
            <article class="timeline-item">
              <div class="timeline-content card">
                <div class="timeline-label">Session {recap.data.sessionNumber} · {recap.data.date}</div>
                <h3>{recap.data.title}</h3>
                <p class="muted">{recap.data.summary}</p>
                <a class="button" href={`/recaps/${recap.slug}`}>Read Full Recap</a>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <>
          <div class="card" style="margin-top:1.5rem;">
            <label class="sr-only" for="recap-search">Search recaps</label>
            <input id="recap-search" type="search" placeholder="Search recaps..." />
            <div class="tag-list" style="margin-top:1rem;">
              <button class="chip active" type="button" data-tag="all">All</button>
              {tags.map((tag) => (
                <button class="chip" type="button" data-tag={tag}>{tag}</button>
              ))}
            </div>
          </div>
          <div id="recap-list" class="grid grid-2" style="margin-top:1.5rem;"></div>
        </>
      )}
    </div>
  </section>
</BaseLayout>

{!useTimelineLayout && (
  <script>
    const recaps = JSON.parse(`${JSON.stringify(timelineRecaps.map((recap) => ({
      title: recap.data.title,
      sessionNumber: recap.data.sessionNumber,
      date: recap.data.date,
      summary: recap.data.summary,
      tags: recap.data.tags,
      slug: recap.slug
    })))`);

    const list = document.getElementById('recap-list');
    const searchInput = document.getElementById('recap-search');
    const tagButtons = document.querySelectorAll('[data-tag]');
    let activeTag = 'all';

    const render = () => {
      const query = searchInput.value.toLowerCase();
      const filtered = recaps.filter((recap) => {
        const matchesTag = activeTag === 'all' || recap.tags.includes(activeTag);
        const matchesQuery = [recap.title, recap.summary, recap.tags.join(' ')].some((field) =>
          field.toLowerCase().includes(query)
        );
        return matchesTag && matchesQuery;
      });

      list.innerHTML = filtered
        .map(
          (recap) => `
          <article class="card">
            <h3>${recap.title}</h3>
            <p class="muted">Session ${recap.sessionNumber} · ${recap.date}</p>
            <p>${recap.summary}</p>
            <a class="button" href="/recaps/${recap.slug}">Read Full Recap</a>
          </article>
        `
        )
        .join('');
    };

    tagButtons.forEach((button) => {
      button.addEventListener('click', () => {
        activeTag = button.dataset.tag;
        tagButtons.forEach((btn) => btn.classList.remove('active'));
        button.classList.add('active');
        render();
      });
    });

    searchInput.addEventListener('input', render);
    render();
  </script>
)}

<style>
  input[type='search'] {
    width: 100%;
    padding: 0.7rem 0.9rem;
    border-radius: 12px;
    border: 1px solid var(--border);
    background: var(--surface-alt);
    color: inherit;
  }

  .chip.active {
    outline: 2px solid var(--accent);
  }

  .muted {
    color: var(--muted);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    border: 0;
  }

  .timeline {
    display: grid;
    gap: 2rem;
    position: relative;
    padding-left: 1.5rem;
    overflow: visible;
  }

  .timeline::before {
    content: "";
    position: absolute;
    left: 0.4rem;
    top: 0;
    bottom: 0;
    width: 2px;
    background: linear-gradient(180deg, transparent, var(--accent), transparent);
    opacity: 0.5;
    z-index: 0;
  }

  .timeline-item {
    display: block;
    padding-left: 1.5rem;
    position: relative;
    z-index: 1;
  }

  .timeline-item::before {
    content: "";
    position: absolute;
    left: -0.1rem;
    top: 50%;
    transform: translateY(-50%);
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 18px rgba(56, 189, 248, 0.6);
    z-index: 2;
  }

  .timeline-content {
    display: grid;
    gap: 0.75rem;
    position: relative;
    z-index: 1;
  }

  .timeline-label {
    font-size: 0.85rem;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  @media (max-width: 520px) {
    .timeline {
      --timeline-gutter: 22px;
      padding-left: var(--timeline-gutter);
      overflow: visible;
    }

    .timeline::before {
      left: calc(var(--timeline-gutter) / 2);
    }

    .timeline-item {
      padding-left: 0;
    }

    .timeline-item::before {
      left: calc(-1 * var(--timeline-gutter) / 2 - 7px);
    }
  }
</style>
